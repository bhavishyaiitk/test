{
  "version": 2,
  "builds": [
    {
      "src": "app.js",          // Change this if your main file is named differently
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/app.js"        // Change this to match your main file
    }
  ]
}
